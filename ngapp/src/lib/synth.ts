import PlaybackHandle from "./synth/playbackHandle";
import Buttons from "./synth/buttons";
import Settings from "./synth/settings";

const synth = {
  PlaybackHandle, Buttons, Settings,
}

export default synth;