import { TsDirective } from './ts.directive';

describe('TsDirective', () => {
  it('should create an instance', () => {
    const directive = new TsDirective();
    expect(directive).toBeTruthy();
  });
});
