
  function (req, res) {
  Story.find({"author": req.params.author, date: {$gte: req.params.date}}, function (err, stories) {
    if(err) {
      console.log(err);
      res.json(err)
    } else {
      res.json(stories);
    }
  });
